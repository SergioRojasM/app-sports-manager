## 1. Setup

- [x] 1.1 Create a new branch `fix/defer-plan-purchase-until-booking-completes` from the current base branch — created from `develop`
- [x] 1.2 Verify the working branch is not `main`, `master`, or `develop` before making any changes — confirmed `fix/defer-plan-purchase-until-booking-completes`

## 2. Database Migration

- [x] 2.1 Create `supabase/migrations/{timestamp}_defer_plan_purchase_until_reserva.sql` — created as `20260831120000_defer_plan_purchase_until_reserva.sql`
- [x] 2.2 In the migration, drop the current `book_and_deduct_service_units(uuid, uuid, uuid, uuid, text, jsonb, uuid, jsonb, boolean, uuid)` overload (the signature from `20260813180000_omitir_confirmacion_plan.sql`)
- [x] 2.3 Recreate `book_and_deduct_service_units` (`create or replace function`) with the same parameters plus a new trailing `p_plan_purchase jsonb default null`, carrying forward the entire existing function body (profile-completeness gate, formulario respuesta insert, pass-1 deductions pre-validation, reservas insert, pass-2 deductions) unchanged. **Verified rather than assumed**: dumped the live function via `pg_get_functiondef` and diffed it against the `20260813180000` file body — identical, and exactly one overload live — then assembled the new migration programmatically from that verified body (declares + statements spliced around the new block) and re-diffed the result to confirm the carried-forward portion is byte-identical. This avoided the stale-base defect class that bit the previous change (a view recreated from an outdated definition silently dropped a column).
- [x] 2.4 At the top of the function body, before any existing logic runs, add the `if p_plan_purchase is not null then ... end if;` block: raise `PLAN_PURCHASE_REQUIERE_PENDIENTE` if `p_permitir_pendiente` is false; raise `PLAN_NO_DISPONIBLE` if `public.can_subscribe_to_plan((p_plan_purchase->>'plan_id')::uuid, p_tenant_id)` is false; raise `SUSCRIPCION_PENDIENTE_EXISTENTE` if a `pendiente` subscription already exists for `(p_atleta_id, plan_id)`; otherwise insert `suscripciones` (`pendiente`), populate `suscripcion_servicios` from `plan_tipos_servicios` (mirroring `populate_suscripcion_servicios`'s insert-select, `on conflict do nothing`), insert `pagos` (`pendiente`, `comprobante_path = null`), and set `p_suscripcion_id` to the new subscription's id so the existing reservation-insert logic below picks it up unchanged
- [x] 2.5 Keep `security definer` and `set search_path = public` on the recreated function — verified live: `prosecdef = t`, `proconfig = {search_path=public}`
- [x] 2.6 Apply the migration to the **local** Supabase instance only — never push to the remote/hosted project as part of this change — and verify it applies cleanly. Applied via `psql` into the local `supabase_db_app_sports_manager` container (the `supabase` CLI is not on PATH in this environment) and recorded version `20260831120000` in `supabase_migrations.schema_migrations` so the CLI stays in sync. Live signature confirmed: one overload, now ending in `p_plan_purchase jsonb`.
- [x] 2.7 Manually exercise the new function via `psql`/SQL editor against local data — all scenarios run inside `begin … rollback` so the dev DB was left untouched (verified: zero residual rows afterward). Fixtures were production-shaped: the public plan `SWIMFEST - NO MEMBERS` grants exactly the one service (`SWIMFEST`, 1 unit) that the `omitir_confirmacion_plan = true` training requires. Results: (a) `p_plan_purchase = null` → `confirmada` reserva, no subscription created, legacy path unchanged; (b) valid purchase → reserva `pendiente` linked to the new subscription, subscription `pendiente` with comentarios, `suscripcion_servicios` mirroring `plan_tipos_servicios` (1/1 units), pago `pendiente` 125000 with null `comprobante_path`, and **zero** `reserva_servicios` rows (no unit deducted, as intended); (c) a plan the athlete can't subscribe to → `PLAN_NO_DISPONIBLE`; (d) pre-existing `pendiente` subscription → `SUSCRIPCION_PENDIENTE_EXISTENTE`. **Two scenarios added beyond the checklist**: (e) a purchase passed with `p_permitir_pendiente = false` → `PLAN_PURCHASE_REQUIERE_PENDIENTE`; (f) **the atomicity guarantee itself** — forced the reservation to fail *after* the purchase block had run (via a formulario requiring profile fields the athlete lacks, raising `PERFIL_INCOMPLETO`) and confirmed the subscription count returned to 0, proving the purchase rolls back with the reservation.

## 3. Types

- [x] 3.1 Add `PendingPlanPurchaseDraft` to `src/types/portal/suscripciones.types.ts`: `{ planId: string; planTipoId: string | null; comentarios: string | null; metodoPagoId: string; monto: number; file: File | null }`
- [x] 3.2 In `src/types/portal/reservas.types.ts`, replace `CreateReservaInput.plan_pendiente_suscripcion_id?: string | null` with `plan_pendiente_compra?: { plan_id: string; plan_tipo_id: string | null; comentarios: string | null; metodo_pago_id: string; monto: number } | null`

## 4. Service Layer

- [x] 4.1 In `src/services/supabase/portal/reservas.service.ts`'s `create()`, change the `book_and_deduct_service_units` RPC call: keep `p_suscripcion_id: null` always, and pass `p_plan_purchase` built from `input.plan_pendiente_compra` (mapped 1:1 to the RPC's expected keys: `plan_id`, `plan_tipo_id`, `comentarios`, `metodo_pago_id`, `monto`) only when `pendienteSinPlan` is true and `input.plan_pendiente_compra` is present; otherwise `null`. Confirmed the surrounding logic stays coherent: on the pending path `validateBookingRestrictions` returns `matchedRow: null`, so `serviceSlots`/`deductions` are empty and no unit deduction is attempted — matching the DB-level test that showed zero `reserva_servicios` rows.
- [x] 4.2 Add error-code mappings alongside the existing `SUSCRIPCION_INACTIVA`/`UNIDADES_AGOTADAS` handling: `PLAN_NO_DISPONIBLE` → `{ ok: false, code: 'SERVICIO_REQUERIDO', message: 'El plan seleccionado ya no está disponible. Elige otro plan para continuar.' }`; `SUSCRIPCION_PENDIENTE_EXISTENTE` → `{ ok: false, code: 'SERVICIO_REQUERIDO', message: 'Ya tienes una solicitud de plan pendiente de aprobación. Espera a que sea revisada antes de continuar.' }`

## 5. Hooks

- [x] 5.1 In `src/hooks/portal/planes/useSuscripcion.ts`, re-type the `onSubscribed` option to `(purchase: PendingPlanPurchaseDraft) => void`
- [x] 5.2 In `submit()`, when `onSubscribed` is provided: after existing subtype/duplicate validation passes, skip the `suscripcionesService.createSuscripcion` / `pagosService.createPago` / file-upload steps entirely, call `onSubscribed({ planId: selectedPlan.id, planTipoId: selectedTipo?.id ?? null, comentarios: data.comentarios.trim() || null, metodoPagoId: data.metodo_pago_id, monto: selectedTipo?.precio ?? 0, file: data.file })`, close the modal, and return `true`. When `onSubscribed` is omitted, `submit()` behavior is unchanged (immediate creation)
- [x] 5.3 In `src/hooks/portal/entrenamientos-publicos/usePublicTrainingReserva.ts`, rename state `pendingPlanSuscripcionId: string | null` to `pendingPlanPurchase: PendingPlanPurchaseDraft | null`
- [x] 5.4 Rename `continueWithPendingPlan(suscripcionId: string)` to `continueWithPendingPlan(purchase: PendingPlanPurchaseDraft)`; it stores the draft and opens the booking form exactly as before, with no DB call
- [x] 5.5 In `onCreateReserva`, when `pendingPlanPurchase` is set, build `plan_pendiente_compra` from it (`plan_id: pendingPlanPurchase.planId`, `plan_tipo_id: pendingPlanPurchase.planTipoId`, `comentarios: pendingPlanPurchase.comentarios`, `metodo_pago_id: pendingPlanPurchase.metodoPagoId`, `monto: pendingPlanPurchase.monto`) and pass it alongside `permitir_pendiente_sin_plan: true` to `reservasService.create()`
- [x] 5.6 After a successful create when `pendingPlanPurchase?.file` is present: query `pagos` for `id` by `suscripcion_id = result.suscripcion_id` (`.maybeSingle()`), then call `storageService.uploadPaymentProof(...)` + `pagosService.updateComprobantePath(...)`, wrapped in try/catch so a failure never fails the overall booking success state
- [x] 5.7 Reset `pendingPlanPurchase` to `null` in `openBooking()` alongside the existing state resets

## 6. Components

- [x] 6.1 In `src/components/portal/entrenamientos-publicos/PublicTrainingReservaModal.tsx`, update the `PlanesPublicosModal`'s `onSubscribed` prop wiring to `(purchase) => void reserva.continueWithPendingPlan(purchase)`
- [x] 6.2 In `src/components/portal/planes-publicos/PlanesPublicosModal.tsx`, re-type the `onSubscribed` prop to `(purchase: PendingPlanPurchaseDraft) => void` — pure passthrough to `useSuscripcion`, no other change
- [x] 6.3 Confirm `SuscripcionModal.tsx` needs no changes (it only collects form data and calls `onConfirm`, unaware of persistence timing) — verified: it imports no service and performs no writes; `isSubmitting` only disables controls (in the deferred path it stays false and the modal closes immediately, so there is no "Enviando..." flash). Also confirmed its Confirmar button already requires a payment method, so `metodo_pago_id` always reaches the RPC's `::uuid` cast as a real value.

## 7. Documentation

- [x] 7.1 Update `projectspec/03-project-structure.md` entries for every modified file in this change (migration, types, service, hooks, components) — updated 8 entries: `PlanesPublicosModal`, `PublicTrainingReservaModal`, `useSuscripcion`, `usePublicTrainingReserva`, `reservas.service.ts`, `reservas.types.ts`, `suscripciones.types.ts`, and the `book_and_deduct_service_units` row in the RPC reference table. The doc does not track migration filenames individually, so the RPC row carries the DB change.

## 7b. Follow-up requested during implementation

- [x] 7b.1 Add follow-up links to the final confirmation screen in `PublicTrainingReservaModal.tsx`: "Puedes consultar tus entrenamientos en: **Mis Reservas**" (`/portal/mis-reservas`) and "Puedes consultar tus suscripciones en: **Mis Suscripciones**" (`/portal/mis-suscripciones`), in a bordered block under the existing confirmation text. Both targets are the **cross-tenant** athlete routes from `resolvePortalMenu`'s no-tenant menu — the tenant-scoped `/portal/orgs/{id}/mis-reservas` variants would be wrong here, since a marketplace booker is frequently not a member of the organization. Labels use the app's own section names ("Mis Reservas" / "Mis Suscripciones") rather than the literal wording of the request, so the links match the nav items and page headings they lead to. Verified in the browser: both links render, are labeled correctly, navigate successfully, and the just-created booking and plan request are actually visible on the two destination pages.

## 8. Verification & Handoff

All browser testing below drove the real UI in headless Chromium against the local dev
server + local Supabase, as a real logged-in athlete (a freshly created, email-confirmed
non-member of the tenant, with a completed profile so the US-0095 profile gate wouldn't
mask the flow under test). Fixture: the `omitir_confirmacion_plan = true` SWIMFEST
publication, whose required service (`SWIMFEST`, 1 unit) is granted by exactly one public
plan (`SWIMFEST - NO MEMBERS`, $125.000) — the real production shape of this feature.

Two fixture values had to be changed to reach the code under test, and **both were
restored afterward (verified)**: the training's `fecha_hora` (its real date is ~30h away
and the training enforces 30h booking notice, so every attempt was rejected with
`TIMING_RESERVA` before reaching the plan check) and, for 8.7 only, the
`omitir_confirmacion_plan` flag. All rows, the storage object and the test user were
deleted at the end; final state re-verified as clean.

- [x] 8.1 Manual test: on a training with `omitir_confirmacion_plan = true`, select a plan and confirm the payment form, then close the modal before finishing the booking — verify via direct DB query that zero `suscripciones`/`suscripcion_servicios`/`pagos` rows were created. **PASSED** — after clicking "Confirmar" on the plan form the DB was still `{suscripciones:0, pagos:0, suscripcion_servicios:0, reservas:0}`, and still all zeros after closing the tab. Pre-fix this same click wrote 1/1/1. The assertion is not vacuous: the UI *did* advance to the "Nueva Reserva" booking form, which only happens via the `onSubscribed` callback — so the purchase was captured, just not persisted.
- [x] 8.2 Manual test: retry the same booking afterward — verify the athlete sees the same fresh eligibility rejection as a first-time visitor, with no "Ya tienes una solicitud pendiente" block. **PASSED** — retry after a real abandonment showed no duplicate-request warning, and "Confirmar" went from disabled (no payment method chosen) to enabled, i.e. the retry can actually proceed. This is the exact dead end the bug used to create.
- [x] 8.3 Manual test: complete the full flow end-to-end — **PASSED**. Drove plan → payment form → booking form → the mandatory 9-field formulario (6 text + 3 button-group selections) → "Guardar y reservar", landing on "Tu reserva y tu solicitud de plan quedaron pendientes de aprobación." Resulting rows: exactly one each of `suscripciones`/`suscripcion_servicios`/`pagos`/`reservas`; `reserva.estado = pendiente` with `suscripcion_id` linking the new subscription; `suscripcion.estado = pendiente`; `pago.estado = pendiente` with `monto = 125000.00`; the comment typed in the plan form persisted; the formulario response linked. **No unit deducted** (`reserva_servicios` empty, `unidades_restantes` still 1) — correct while pending. Atomicity evidence: `created_at` spread across all four rows was **0.000s**.
- [x] 8.4 Manual test: repeat 8.3 with a proof-of-payment file attached — **PASSED**. Attaching the file still wrote nothing at plan-confirm time; after the booking succeeded, `comprobante_path` was set to `orgs/<tenant>/users/<athlete>/receipts/<pago_id>.png`, the object was confirmed present in `storage.objects`, and the pago remained `pendiente`.
- [x] 8.5 Manual test: standalone catalog purchase still creates immediately, unchanged — **PASSED**. Used `/portal/orgs` → "Ver planes" (`VerPlanesButton` → `PlanesPublicosModal` with **no** `onSubscribed`): clicking "Confirmar" created the `suscripciones` + `pagos` + `suscripcion_servicios` rows immediately, created no reservation, and showed the generic "El administrador revisará tu suscripción" message. This proves the new branch discriminates correctly between the two call sites rather than deferring everywhere. (Checked all three `PlanesPublicosModal` call sites — `VerPlanesButton` and `PublicTrainingCard` both omit `onSubscribed`, so both keep the old behavior.)
- [x] 8.6 Manual test: existing admin approve/reject flows still work against a subscription created via the new atomic path — **PASSED**, exercised at the DB level against a subscription created through the new `p_plan_purchase` path. Approval (`estado → activa` + `confirm_pending_reservas_for_suscripcion`) moved the reservation `pendiente → confirmada`, deducted the unit (1 → 0) and wrote the `reserva_servicios` ledger row; rejection (`reject_pending_reservas_for_suscripcion`) moved it to `rechazada` with `motivo_rechazo = 'Comprobante ilegible'`, and that rejected reservation correctly counted as **not** occupying a spot. **Scope note**: the cascade RPCs and service layer were exercised, not the `ValidarSuscripcionModal`/`ValidarPagoModal` screens themselves (they need a tenant-admin account and are untouched by this change — a subscription from the new path is an ordinary `pendiente` row, indistinguishable from one created the old way).
- [x] 8.7 Manual test: a training with `omitir_confirmacion_plan = false` is unaffected — **PASSED**. With the flag flipped off, the dialog reverted to the blocking "No puedes reservar todavía" + plain "Ver planes de Wolfpack" (no "Elegir plan y continuar"), and buying a plan there persisted the purchase immediately, created **no** reservation, and ended at the generic review message — i.e. exactly the pre-US-0110 blocking behavior.
- [x] 8.8 Run `npx tsc --noEmit` and `eslint` on all new/modified files and confirm 0 errors/problems; run the test suite if one exists for touched files. Do not run a full build — `npx tsc --noEmit` passes repo-wide (exit 0); `eslint` on all 7 modified source files reports 0 problems. No test suite exists in this repo (no `test` script, zero `*.test.*`/`*.spec.*` files), so there was nothing to run. Full build deliberately not run.
- [x] 8.9 Draft a commit message and pull request description summarizing the change for the user (commits are only made when the user explicitly asks) — written to `HANDOFF.md` in this change directory. **Nothing has been committed**; the branch `fix/defer-plan-purchase-until-booking-completes` holds the working-tree changes for the user to review first.
